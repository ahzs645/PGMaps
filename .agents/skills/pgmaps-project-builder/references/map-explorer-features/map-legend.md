# `map-legend`

Component: `src/maps/project-explorer/features/MapLegendFeature.tsx`

```json
{ "type": "map-legend", "title": "Categories", "description": "Marker composition and totals." }
```

This is a map-only feature. It uses configured category order/colours and
adapter counts through shared `MapLegendPanel` and `LegendItem`. Elevate it when
the timeline is open so overlay offsets remain consistent.
