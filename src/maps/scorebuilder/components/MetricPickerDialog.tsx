/**
 * The add-metric dialog now lives with the rest of the metric catalogue so the
 * dialog, the Build library, and the Explore library stay in lockstep — most
 * importantly on which metrics are unavailable for the active study area.
 */
export { MetricPickerDialog } from './MetricLibrary'
