import {
  getWatershedLevelSourceNote,
  loadStudyAreaRegions,
  useStudyAreaRegions,
} from '@/lib/studyArea'

export {
  getWatershedLevelSourceNote,
  loadStudyAreaRegions,
  useStudyAreaRegions,
}

// Backward-compatible alias for existing score-builder imports.
export const useScoreBuilderRegions = useStudyAreaRegions
